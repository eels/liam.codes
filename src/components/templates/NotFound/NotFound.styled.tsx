import styles from './NotFound.module.scss';
import { create } from 'chic-modules';

const styled = create(styles);

export const Spacer = styled.div('spacer');

export const Link = styled.div('link');
